@model WebUI.Areas.Admin.Models.Sube.SubeDto
@{
    Layout = null;
    string AreaName = "Admin";
}

<div class="modal show" id="editModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered modal" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabel">Şube</h5><button class="btn-close" onclick="zuraf.closeModal('#editModal')" type="button" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <form id="editForm" method="post" onsubmit="return false" action="/@AreaName/save-sube" enctype="multipart/form-data">

                    @Html.HiddenFor(Model => Model.Id)
                    <div class="row">
                        <div class="col-md-12">
                            <div class="form-floating mb-3">
                                @Html.TextBoxFor(Model => Model.Subeisim, new
                                    {
                                        @class = "form-control",
                                        id = "name"
                                    })
                                <label for="name">Şube Adı</label>
                                <div class="invalid-feedback">
                                    @Html.ValidationMessageFor(Model => Model.Subeisim) 
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="row">
                        <div class="col-md-12">
                            <div class="form-floating mb-3">
                                @Html.TextAreaFor(Model => Model.Adres, new
                                    {
                                        @class = "form-control",
                                        id = "name"
                                    })
                                <label for="name">Adres</label>
                                <div class="invalid-feedback">
                                    @Html.ValidationMessageFor(Model => Model.Adres) 
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="row">
                        <div class="col-md-12">
                            <div class="form-floating mb-3">
                                @Html.TextAreaFor(Model => Model.TelefonNumarasi1, new
                                    {
                                        @class = "form-control",
                                        id = "name"
                                    })
                                <label for="name">Telefon Numarası</label>
                                <div class="invalid-feedback">
                                    @Html.ValidationMessageFor(Model => Model.TelefonNumarasi1) 
                                </div>
                            </div>
                        </div>
                    </div>
                </form>

            </div>
            <div class="modal-footer">
                <button onclick="zuraf.closeModal('#editModal')" class="btn btn-secondary btn-sm" type="button" data-dismiss="modal">Kapat</button>
                <button id="saveButton" class="btn btn-primary btn-sm" type="button">Kaydet</button>
            </div>
        </div>
    </div>
</div>

<script>


    $(document).ready(function () {

        $('#saveButton').on('click', function () {

            zuraf.submitForm("editForm");
        });

    });
</script>